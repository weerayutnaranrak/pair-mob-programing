/**
 * Mob Exercise 1:
 * Return the arithmetic mean of a non-empty array of finite numbers.
 *
 * Requirements:
 * - Throw if input is not an array
 * - Throw if array is empty
 * - Throw if any item is not a finite number
 */
export function mean(input: unknown): number {
  // TODO: implement
  throw new Error("Not implemented");
}